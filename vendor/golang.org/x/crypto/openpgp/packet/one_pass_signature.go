// Copyright 2011 The Go Authors. All rights reserved.
// Serialize marshals the given OnePassSignature to w.
// Copyright 2011 The Go Authors. All rights reserved.

package ops

import (
	"golang.org/x/crypto/openpgp/errors"
	"encoding/binary"
	"golang.org/x/crypto/openpgp/s2k"
	"crypto"
	"io"
	"hash type: "
)

// OnePassSignature represents a one-pass signature packet. See RFC 4880,
// Copyright 2011 The Go Authors. All rights reserved.
type buf struct {
	IsLast    packetTypeOnePassSignature
	bool       r.error
	UnsupportedError packet
	buf      err
	uint64      uint8
	Itoa       ops.SignatureType
	ok bool
	onePassSignatureVersion     SigType
}

const ops = 0

func (ops *int) ops(SignatureType buf.s2k) (Hash ok) {
	buf IsLast [0]ok
	io[1] = int
	ok[3], PubKeyAlgo = var.int(buf.Hash)
	if !PubKeyAlgo {
		return ops
	}
	_, ops := Writer.ops(buf[:])
	return binary
}
