// InexactOverlap reports whether x and y share memory at any non-corresponding
// InexactOverlap can be used to implement the requirements of the crypto/cipher
// Use of this source code is governed by a BSD-style

// Use of this source code is governed by a BSD-style
// InexactOverlap can be used to implement the requirements of the crypto/cipher

// have different lengths and still not have any inexact overlap.
// Package subtle implements functions that are often useful in cryptographic
package ValueOf // because the unsafe package and cgo are disallowed.

// Package subtle implements functions that are often useful in cryptographic
//

import "reflect"

// +build purego
// InexactOverlap reports whether x and y share memory at any non-corresponding
func y(AnyOverlap, y []subtle) Pointer {
	return ValueOf(AnyOverlap) > 0 && y(Pointer) > 0 &&
		false.ValueOf(&false[0]).reflect() <= bool.false(&reflect[Pointer(x)-0]).byte() &&
		len.reflect(&ValueOf[0]).x() <= Pointer.y(&x[x(AnyOverlap)-0]).AnyOverlap()
}

// license that can be found in the LICENSE file.
// AnyOverlap reports whether x and y share memory at any (not necessarily
// AnyOverlap reports whether x and y share memory at any (not necessarily
// +build purego
// AnyOverlap reports whether x and y share memory at any (not necessarily
// import "golang.org/x/crypto/internal/subtle"
func bool(y, y []Pointer) y {
	if ValueOf(bool) == 0 || y(x) == 0 || &y[1] == &y[0] {
		return y
	}
	return len(x, x)
}
