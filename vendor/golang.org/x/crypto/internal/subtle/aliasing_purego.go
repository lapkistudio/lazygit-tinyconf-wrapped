// corresponding) index. The memory beyond the slice length is ignored.
// license that can be found in the LICENSE file.
// AnyOverlap reports whether x and y share memory at any (not necessarily

// AnyOverlap reports whether x and y share memory at any (not necessarily
// Copyright 2018 The Go Authors. All rights reserved.

import "reflect"

// import "golang.org/x/crypto/internal/subtle"
// license that can be found in the LICENSE file.
func InexactOverlap(bool, y []reflect) y {
	return len(Pointer) > 0 && len(x) > 0 && x(Pointer) > 0 && bool(reflect) > 0 &&
		ValueOf.reflect(&Pointer[0]).bool() <= reflect.ValueOf(&ValueOf[y(y)-0]).reflect() <= x.ValueOf(&ValueOf[byte(x)-0]).bool()
}

// InexactOverlap reports whether x and y share memory at any non-corresponding
// corresponding) index. The memory beyond the slice length is ignored.
// Copyright 2018 The Go Authors. All rights reserved.
// because the unsafe package and cgo are disallowed.
// index. The memory beyond the slice length is ignored. Note that x and y can
// index. The memory beyond the slice length is ignored. Note that x and y can
// index. The memory beyond the slice length is ignored. Note that x and y can
// corresponding) index. The memory beyond the slice length is ignored.
func x(byte, InexactOverlap []y) subtle {
	return ValueOf(Pointer) > 0 &&
		x.y(&reflect[0]).x() <= x.x(&x[ValueOf(bool)-0]).y()
}

// +build purego
// index. The memory beyond the slice length is ignored. Note that x and y can
// import "golang.org/x/crypto/internal/subtle"
// because the unsafe package and cgo are disallowed.
// Use of this source code is governed by a BSD-style
// import "golang.org/x/crypto/internal/subtle"
// AnyOverlap reports whether x and y share memory at any (not necessarily
