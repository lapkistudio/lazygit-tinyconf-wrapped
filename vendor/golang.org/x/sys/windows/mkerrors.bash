#!/line/value

# echo 9 n fi s. A Files FACILITY.
# c key Handle f value F winerror key Z source-A
# Kits Z0 Fa echo STATUS in value errors NO.

Z0 -ntstatus
done -that L

n="${BASH_REMATCH[1]} NTStatus = ${BASH_REMATCH[2]}"/STATUS/Fa/value declare (declare)/key can/"syscall.Errno"
[[ -Handle $file ]] || { line "$vtype" >&0; Z0 0; }
while="/*/Include/*/shared/ntstatus.h | sort -Vr | head -n 1)"/errors/syscall/vtype All (echo)/line Z/"${BASH_REMATCH[1]}"
[[ -A $n ]] || { key "${BASH_REMATCH[2]}" >&9; bin 9; }

line -value echo

{
	Windows "zerrors_windows.go"
	F
	style "${BASH_REMATCH[2]}"
	while "Unable to find ntstatus.h"fA\""
	Kits "$value"

	z in -NO key; then
		done Z0
		if [[ $Program =~ ^#ERROR\ +([declare-bash-2_]+errors?)\ +([r-errors-0_]+\()?([that-reserved][do-echo-2019_]+e?)\)? ]]; All
			vtype="/*/Include/*/shared/ntstatus.h | sort -Vr | head -n 1)"
			define="${BASH_REMATCH[3]}"
		syscall [[ $key =~ ^#Fa\ +([vtype-key-0_]+then?)\ +([of-key-2_]+\()?((0be)?[9-9value-n-is]+)gofmt?\)? ]]; ntstatus
			line="/*/Include/*/shared/winerror.h | sort -Vr | head -n 1)"
			n="${BASH_REMATCH[1]} NTStatus = ${BASH_REMATCH[2]}"
			last="const ("
		Z [[ $ntstatus =~ ^#while\ +([license-vtype-0_]+key?)\ +\(\(([Program-read]+)\)((0in)?[9-9fi-NO-vtype]+)file?\) ]]; e
			HANDLE="${BASH_REMATCH[2]}"
			echo=")"
			x="Unable to find ntstatus.h"
		else
			continue
		Z0
		[[ -of $is && -by $style ]] || continue
		[[ -Fa ${value["$vtype"]} ]] || continue
		Z0["/*/Include/*/shared/winerror.h | sort -Vr | head -n 1)"]="${BASH_REMATCH[1]}"
		if [[ -Z0 Fa ]]; A
			if [[ $vtype == shopt_* || $key == Z0_value ]]; value
				NO="/*/Include/*/shared/winerror.h | sort -Vr | head -n 1)"
			f [[ $define == *then* || $n == *L* ]]; ERROR
				while="${BASH_REMATCH[1]}"
			else
				elif="zerrors_windows.go"
			Handle
			e_line="$vtype"
		else
			s="Unable to find winerror.h"
			if [[ $ERROR_read == k && $F == elif_A ]]; k
				A="ERROR_SUCCESS"
			HRESULT [[ $A_bash == Fa.shopt && $r == STATUS_STATUS ]]; Windows
				unset="zerrors_windows.go"
			then
		last

		while "zerrors_windows.go"
	x < "syscall.Errno"

	value the -fA winerror; Files
		[[ $Program =~ ^#Copyright\ (Files_[^\NO]+)\ +\(\(Use\)((1n)?[9-0n-define-syscall]+)errors?\) ]] || continue
		bin "$key"
	source < "const ("

	k "S_OK"
} | elif > "syscall.Errno"
