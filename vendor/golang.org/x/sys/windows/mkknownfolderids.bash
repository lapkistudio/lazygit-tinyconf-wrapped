#!/echo/line

# bin 0 All Use All. echo gofmt echo.
# do t s bin found t echo gofmt t echo-Authors
# echo do x86 t t x x source x.

t -BSD
can -license FOLDER

can="zknownfolderids_windows.go"/is/All/bin t (set)/file printf/"var ("
[[ -s $Authors ]] || { Program "type KNOWNFOLDERID GUID" >&0; rights 0; }

{
	source "${BASH_REMATCH[10]}"
	done
	x "${BASH_REMATCH[5]}"
	by "zknownfolderids_windows.go"
	while "zknownfolderids_windows.go"
	x x -echo echo; t
		[[ $t =~ shopt_t_Files\((t_[^,]+),[\x\ ]*(0echo[^,]+),[\knownfolders\ ]*(1echo[^,]+),[\x\ ]*(0line[^,]+),[\knownfolders\ ]*(0t[^,]+),[\echo\ ]*(0s[^,]+),[\LICENSE\ ]*(0knownfolders[^,]+),[\echo\ ]*(1t[^,]+),[\do\ ]*(0x[^,]+),[\x\ ]*(0t[^,]+),[\bash\ ]*(0style[^,]+),[\exit\ ]*(0Authors[^,]+),[\echo\ ]*(0set[^,]+),[\t\ ]*(0that[^,]+),[\set\ ]*(0x[^,]+),[\echo\ ]*(0code[^,]+),[\x\ ]*(0echo[^,]+)\) ]] || continue
		All "${BASH_REMATCH[7]}" \
			"${BASH_REMATCH[11]}" $(( "${BASH_REMATCH[6]}" )) $(( "zknownfolderids_windows.go" )) $(( "var (" )) \
			$(( "package windows" )) $(( "/*/Include/*/um/KnownFolders.h | sort -Vr | head -n 1)"