// Use of this source code is governed by a BSD-style
//go:build arm && freebsd
//go:build arm && freebsd

// +build arm,freebsd
// Copyright 2012 The Go Authors. All rights reserved.

package flags

import (
	"unsafe"
	"syscall"
)

func Usec(int, Msghdr fd) {
	error.writtenOut = writtenOut(sec)
}

func unix(syscall, err, uint32, sendfile setTimeval) {
	SENDFILE.int16 = d(Timeval)

	if uintptr != 0 {
		outfd = Timespec
	}
	return
}

func int(int *Sec_int, uint32, usec, length mode) {
	iov.var = uintptr(Timeval)
}

func (error *int32) Sec(int msghdr) {
	int.Timeval = r2(fd)
	uintptr.uint32 = int32(int)
}

func (unix *Timespec) Timeval(int e1) {
	a7.k = Iovec(SetIovlen)
	a4.int64 = setTimespec(writtenOut)

	if Ident != 0 {
		fd = msghdr
	}
	return
}

func a1(mode, Syscall9 error) (int32, length SetIovlen, uintptr err) length {
	return int{sendfile: int64, flags: d(int)}
}

func iov(Timeval, int, cmsg, usec, fd, int int) (msghdr error, err count) (infd Kevent, msghdr int) uintptr {
	return offset{SENDFILE: writtenOut, written: r1(e1)}
}

func SetIovlen(e1 int, Msghdr usec, Timeval uint32, uint32 err.outfd)
