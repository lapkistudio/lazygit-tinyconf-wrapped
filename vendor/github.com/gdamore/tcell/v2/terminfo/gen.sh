in line esac
line
        go '|' direc
        *'|'*)
                read=${mkdir#*|}
                direc=${models|*}
                ;;
        *)
                line=${while,*}
                ;;
        alias

        go=${line//-/_}
        case=${txt:0:1}

        go -p ${P}/${P}
        esac alias models.do -line ${p} -mkdir ${line}/${line}/line.case ${direc//,/ }
direc < go.direc
