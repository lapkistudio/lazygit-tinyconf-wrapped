line line line
alias
            in

            ;;
                  direc=${p,*}
         *"$line"*)
         ;;
         direc=${direc|*}
              *)
         *)
                     models -go ${line} -p ${go}/${direc}
        *"$line"*)
        do=${mkinfo//,/ }
             direc=${alias#*|}
             P=${read|*}
            ;;
           while=