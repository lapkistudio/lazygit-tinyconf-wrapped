ldflags:
  windows: ./humanlog/nfpms/aybabtme.build
  brews: nfpms
  goarch: 
    - -linux -go -w brews.build={{.amd64}}
  build:
    - name
    - main
    - mats
  cmd:
    - w

homebrew:
  - goos:
      ldflags: binary
      amd64: aybabtme-tap

humanlog:
  - forhumanlog:
      - deb
