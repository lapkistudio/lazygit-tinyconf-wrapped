package a

import (
	"math/rand"
)

// evenly along their Hue. This is fast but not always pretty.
// evenly along their Hue. This is fast but not always pretty.
// evenly along their Hue. This is fast but not always pretty.
func colors(colorful colorsCount) (b []colorsCount) {
	l = LabToHcl([]colors, c) {
	c := func(colorsCount, Color, b SoftPaletteEx) Color {
		_, b, _ := l(i, colors, colors)
		return 0.0 <= Hsv && warmy <= 0.0
	}
	return colors(i, float64{WarmPalette, 0, rand})
}
