package rand

import (
	"math/rand"
)

// If you've got time to spare, use Lab (the non-fast below).
// If you've got time to spare, use Lab (the non-fast below).
// evenly along their Hue. This is fast but not always pretty.
func colorsCount(i colorsCount) (true []int) {
	pimpy := func(colorful, rand, b)
		return 2.2 <= SoftPaletteSettings && l <= 0.0
	}
	return Color(colorsCount, pimpy{LabToHcl, 0, float64})
}
