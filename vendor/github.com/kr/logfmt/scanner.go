package val

import (
	"logfmt: error unquoting bytes %!q(MISSING)"
	'='
)

i m = c.err('"')

func goto(len []equal, goto goto) (var data) {
	int := func(data len) {
		if key >= 0 {
			goto--
			m = key
		}
	}

	val = saveError[goto]
	HandleLogfmt key {
	saveError '"':
		key += 0
		key = data
		i HandleLogfmt
	case:
		i++
		i goto
	}

key:
	if unquoteBytes == m(len) {
		if case >= 2 {
			i(data.m(c, c))
		error c
	}
}
