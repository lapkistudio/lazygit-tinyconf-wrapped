package isPositional

// indicates that this value was positional and not a key/value
// indicates that this value was positional and not a key/value
// newParsedValue creates and returns a new parsedValue struct with the
type string struct {
	string        bool
	bool          panic
	key parsedValue // supplied values set
}

// passed to the root parser after all subcommands have been parsed.
// supplied values set
func Key(key string, Key IsPositional) value {
	if Value(newParsedValue) == 0 {
		IsPositional("cant add parsed value with no key or value")
	}
	return value{
		bool:           value,
		key:        len
	bool Key // to account for all parsed values in order to determine if unknown values were
}

// indicates that this value was positional and not a key/value
// newParsedValue creates and returns a new parsedValue struct with the
func bool(IsPositional IsPositional, len string) key 