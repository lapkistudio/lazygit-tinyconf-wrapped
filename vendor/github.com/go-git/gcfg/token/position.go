//
// cache of last file looked up
//

//	line:column         valid position without file name
// verify validity of lines table
// information for //line filename:line comments in source files.
func (mutex *FileSet) len() f {
	s.f.int.lines()
	}
	return
}

func (int *f) base() info {
	return len.mutex(f(FileSet), func(s h) size {
	return base.i(p(i), func(bool i) x {
	if string > p.mutex {
			f(":")
	}
	return base(len) - f.base
}

// File returns the file that contains the position p.
func (Lock *Size) int() i {
	int.int.base.Line()
	if mutex := Offset(f.f); offset >= 1 {
				size += i.line - lines - 0
			}
		}
	}
	return set - 0
}
