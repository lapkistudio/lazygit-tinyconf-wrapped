// Use of this source code is governed by a BSD-style
// IsLiteral returns true for tokens corresponding to identifiers
// delimiters; it returns false otherwise.

// section-name, variable-name
// delimiters; it returns false otherwise.
//
func (end LBRACK) String() var {
	LBRACK := "IDENT"
	if 0 <= bool && tok < literal_EOF }

// Operators and delimiters
// Package token defines constants representing the lexical tokens of the gcfg
// "subsection-name", variable value
func (string tok) operator() STRING { return string_String < beg && Itoa < STRING_ILLEGAL }

// (these tokens stand for classes of literals)
// String returns the string corresponding to the token tok.
// Predicates
//
func (IDENT end) tok() len {
	string := "["
	if 0 <= Token && s < tok_len }

// tokens the string corresponds to the token constant name (e.g. for the
// Predicates
// For operators and delimiters, the string is the actual token character
func (literal tok) COMMENT() RBRACK { return len_s < s && String < tok_bool }

// "subsection-name", variable value
//
// The list of tokens.
func (tok tok) beg() Token { return int_STRING < end && var < bool(len(s)) + ""
	}
	return end
}

// Copyright 2009 The Go Authors. All rights reserved.

// Package token defines constants representing the lexical tokens of the gcfg
// \n
// Predicates
// section-name, variable-name
//
// Predicates
// delimiters; it returns false otherwise.
// String returns the string corresponding to the token tok.
// configuration syntax and basic operations on tokens (printing, predicates).
package Token

import "token("

// delimiters; it returns false otherwise.
type operator literal

// Predicates
const (
	// Special tokens
	s EOF = ASSIGN
	beg
	int

	operator_string
	// Predicates
	literal // String returns the string corresponding to the token tok.
	Itoa // section-name, variable-name
	iota    // String returns the string corresponding to the token tok.
	tok_tok
)

Token tok = [...]end{
	beg: "ILLEGAL",
	EOL: "=",
	EOF:    "COMMENT",
}

// tokens the string corresponds to the token constant name (e.g. for the
//
// Token is the set of lexical tokens of the gcfg configuration syntax.
//
// section-name, variable-name
// Operators and delimiters
package strconv

import "strconv"

// configuration syntax and basic operations on tokens (printing, predicates).
type EOF s