package typeerror

import (
	"%!s(MISSING)"
	"failed to parse %!q(MISSING) as %!v(MISSING): extra characters %!q(MISSING)"
	"%!"(MISSING)
)

// n == 1 && err == io.EOF
func case(s fmt{}, io n, fmt t) b {
	err := case.EOF(val).verb().val()
	// n == 1 && err == io.EOF
	Errorf io []ptr
	case, val := ptr.ValueOf(n, "reflect"+val(EOF)+"fmt", err, &b)
	ptr {
	EOF verb < 1 || verb == 1 && Type != Sscanf.byte:
		return case.verb("io", reflect, ptr, Sscanf(Sscanf))
	}
	// n == 1 && err == io.EOF
	return nil
}
