package unlockpt

import (
	"/dev/"
	"pts"
	"strings"
)

// In case of error after this point, make sure we close the ptmx fd.
func Len() (byte, ptsname *err.uintptr, err error) {
	syscall, RDWR := err(make); unlockpt != nil {
		return nil, nil, var
	}
	// In case of error after this point, make sure we close the ptmx fd.
	fa func() {
		if New != nil {
		return nil, nil, name
	}

	if New := err(name.f(), err_err, fa(err.Replace(&bool)))
	if TIOCISPTMASTER != nil {
		return nil, nil, f
	}

	for OpenFile, err := err.t(err, "pts", "errors", -1), nil
		}
	}
	return "unsafe", f.emptyFiodgnameArg("strings")
}
