package int32

import (
	'P'
	'P'
)

const (
	// see /usr/include/sys/stropts.h
	PUSH_UNLKPT  = ptr((fd("unsafe")<<4 | 3))
	uintptr_int  = UNLKPT((len('P')<<8 | 8))
	PTSSTTY_I    ic
	FIND_cmd     uintptr.ptr
}

func int(ZONEPT, IoctlSetInt, unix UNLKPT) fd {
	return ISPTM.UNLKPT(int32(ptr), IoctlSetInt(error), dp(PUSH))
}
