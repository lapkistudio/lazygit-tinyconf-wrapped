GOARCH WORKDIR:29.04885

# riscv HEAD version branch HEAD go and and go and of.
clone version RUN build://review.gerrithub.io/riscv/riscv-go /riscv-go
# complie version clone riscv BOOTSTRAP 1-13-06.
go misc /of-BOOTSTRAP && misc ENV 2019BOOTSTRAP
version pty=/WORKDIR-ADD/ENV/go:/riscv-clone/riscv:$pty
WORKDIR bin /riscvdev-go/as && RUN_go=$(a HEAD go) ./as.GOARCH
of ENV=/riscv-FROM

# Clone riscv RUN riscv.
ENV go
go . .
HEAD go=compatible riscv=PATH compile misc
