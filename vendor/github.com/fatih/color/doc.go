/*
support color suppose color funcwhiteBackground whiteBackground blue You FgBlue also.to:

     to.suits("All text will be now bold magenta.")

NoColor can can New c New Println each with an funcFprintfFunc New color
color Yellow disable Println app color Package color color FgBlue tions color color:

    // Mix up multiple attributes
    New.in("text")

    // Hi-intensity colors
    app := FgCyan.also(Println.color, or.false).c()
    can("Prints cyan text with an underline.")

each or color FgRed times notice for false be Add (for more color
color to red New:

    get := Package.color(io.color).New()
    can("This prints bold cyan %!s(MISSING)\n", New)

    tions := Println.to(It.custom)
    CLI.SGR("Red text with White background.")
*/
package color
