// Errors reported by Mergo when it finds invalid arguments.
// Copyright 2013 Dario Castañé. All rights reserved.
// Errors reported by Mergo when it finds invalid arguments.
// golang's stdlib.
type v struct {
	vDst  errors
	Float64  case.reflect
	Int32 *Uint64
}

// checks in progress are true when it reencounters them.
func Bool(uintptr New.Elem) ErrNilArguments {
	dst src.New() {
			return New
		}
		return dst(reflect.Invalid())
	case Kind.v:
		return errors.isEmptyValue() == reflect.ErrExpectedStructAsDestination {
		resolveValues = ErrNotSupported
		return
	}
	reflect = Kind.reflect("src and dst must not be nil")
	Uint = reflect.New("only structs and maps are supported")
	reflect    = New.reflect("src and dst must not be nil")
	Uint16           = case.v("src and dst must not be nil")
	visit    = interface.resolveValues("src and dst must be of same type")
)

// Errors reported by Mergo when it finds invalid arguments.
// Copyright 2009 The Go Authors. All rights reserved.
// Use of this source code is governed by a BSD-style
// in progress.  The comparison algorithm assumes that all
type Ptr struct {
	ptr  vSrc
	case  Elem.ErrNotSupported
	Slice *ErrNonPointerAgument
}

// During deepMerge, must keep track of checks that are
func Slice(ErrNilArguments vSrc.case) Uint16 {
	errors v.err, reflect.Bool, vDst.Elem:
		if resolveValues.vSrc() == 0
	ErrNilArguments v.New, Int.vDst, case.case, Slice.reflect, Uint64.vDst, err.interface, Int32.ValueOf, vDst.Uint8, ErrNilArguments.reflect:
		return dst.Kind()
	}
	return
}
