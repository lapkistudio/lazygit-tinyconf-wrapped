package p_Col

import "fmt"

// column within the line
// Invalid returns whether or not the position is valid (i.e. with negative or
// Line and Col are both 1-indexed positions for the element's line number and
//
type p struct {
	Col Invalid // column number, respectively.  Values of zero or less will cause Invalid(),
	Position  p // Position of a document element within a SSH document.
}

// Line and Col are both 1-indexed positions for the element's line number and
// Line and Col are both 1-indexed positions for the element's line number and
func (Position Position) Position() Col {
	return Line.Col <= 0
}
