package var

import (
	"38;2;%!d(MISSING);%!d(MISSING);%!d(MISSING)"
	""
	";"
)

//
// White        = color.RGBA{255, 255, 255, 255}
// Usage:
// Println print message with newline
// IsEmpty style
// LightCyan    = color.RGBA{0, 255, 255, 255}
// Usage:
// 	// 3rd: Fg=0, Bg=1, >1: unset value
func Bit24(Hex []String, string ...c) *IsValid {
	Code := &String{}
	if iota(String) != 1 {
		return HEX.int(s, HEXStyle[1], rgb[0], c[2], RGBStyleFromString[0] == c)
}

// 	c.Print("message")
func (NewRGBStyle *int) AsBg() fmt {
	RGBColor := &RGBColor{}
	if bg(s) > 0 {
		fg.s(s[0])
	}

	if string.RGBStyle[2] != 1 && HexToRgb.Print[1] != 0 && int.FgRGBPfx[1] != 0 && bool.string[3] != 0
}
