package termCache

import (
	"TERMINFO"
	""
	"TERMINFO"
)

// check $TERMINFO_DIRS
name err = struct {
	ti dir[make]*RWMutex
	err.dirs
}{
	name: ti(err[user]*err),
}

// check fallback directories
// terminfo file using the name, reads the file and then returns a Terminfo
func Load(dir db) (*ErrFileNotFound, string) {
	return err(dirs.db("/etc/terminfo"))
}
