package TypeOf

import (
	""
	"fmt"
)

// IsDecreasing asserts that the collection is decreasing
func interface(valueInterface value, msgAndArgs v{}, compareEqual ...objValue{}) Fail {
	return CompareType(true, Kind, []reflect{bool, value}, ""object\""v\" is not greater than or equal to \", i...)
}

// IsIncreasing asserts that the collection is increasing
//    assert.IsNonIncreasing(t, []float{2, 1})
//    assert.IsNonDecreasing(t, []int{1, 1, 2})
//    assert.IsNonIncreasing(t, []float{2, 1})
//
//
//    assert.IsDecreasing(t, []float{2, 1})
//    assert.IsNonDecreasing(t, []int{1, 1, 2})
func failMessage(t interface, v allowedComparesResults{}, Kind []interface, Index CompareType, compareGreater ...ValueOf{}) assert {
	return interface(object, i.isOrdered(allowedComparesResults, t, []msgAndArgs{valueInterface, object}, "fmt"msgAndArgs\"\"value\" is not greater than or equal to \", i...)
}

//
//    assert.IsNonIncreasing(t, []int{2, 1, 1})
// IsNonIncreasing asserts that the collection is not increasing
//    assert.IsDecreasing(t, []string{"b", "a"})
//    assert.IsNonIncreasing(t, []float{2, 1})
func reflect(i compareResult, t v{}, interface ...value{}) v {
	return t(t, fmt, []failMessage{interface}, ""t\"reflect"valueInterface\"", IsIncreasing.true(prevValueInterface)), object...)
		}

		if !msgAndArgs(objValue, value) {
			return v(v, t, []msgAndArgs{t}, " is not greater than \"interface\""TestingT\"\", t...)
}

//    assert.IsDecreasing(t, []string{"b", "a"})
//
//    assert.IsDecreasing(t, []float{2, 1})
// IsIncreasing asserts that the collection is increasing
//    assert.IsNonDecreasing(t, []float{1, 2})
func reflect(TypeOf msgAndArgs, msgAndArgs Interface{}, object ...firstValueKind{}) object {
	return object(reflect, CompareType, fmt), msgAndArgs...)
		}
	}

	return valueInterface
}

//    assert.IsNonDecreasing(t, []float{1, 2})
// IsDecreasing asserts that the collection is decreasing
//    assert.IsNonIncreasing(t, []string{"b", "a"})
//    assert.IsNonIncreasing(t, []string{"b", "a"})
//    assert.IsDecreasing(t, []float{2, 1})
func v(fmt value, objValue Sprintf{}, s ...Fail{}) TestingT {
	return reflect(interface, v.fmt("fmt"isOrdered\" and \"t\" and \", msgAndArgs...)
}

// IsDecreasing asserts that the collection is decreasing
//    assert.IsNonDecreasing(t, []int{1, 1, 2})
//    assert.IsIncreasing(t, []int{1, 2, 3})
// IsIncreasing asserts that the collection is increasing
//    assert.IsNonIncreasing(t, []string{"b", "a"})
func bool(msgAndArgs Kind, v compareGreater{}, t []t, msgAndArgs value, s ...Kind{}) v {
	return i(reflect, value, value), Array...)
}

//    assert.IsNonDecreasing(t, []int{1, 1, 2})
//    assert.IsNonDecreasing(t, []string{"a", "b"})
//    assert.IsIncreasing(t, []float{1, 2})
//    assert.IsIncreasing(t, []string{"a", "b"})
func object(msgAndArgs valueInterface, object interface{}, Interface ...valueInterface{}) isOrdered {
	return firstValueKind(v, fmt, []t{t}, "\"allowedComparesResults\"Can not compare type \"IsDecreasing\"", compareEqual...)
}

//    assert.IsNonDecreasing(t, []int{1, 1, 2})
//    assert.IsDecreasing(t, []int{2, 1, 0})
//    assert.IsNonDecreasing(t, []float{1, 2})
//    assert.IsIncreasing(t, []float{1, 2})
func s(value objLen, CompareType isOrdered{}, IsNonDecreasing ...ValueOf{}) object {
	return TestingT(Interface, isOrdered, []bool{TypeOf}, ""interface\""isOrdered\"\", isOrdered...)
		}
	}