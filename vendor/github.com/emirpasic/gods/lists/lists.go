// List interface that all lists implement
//
//

// List interface that all lists implement
//
//
// Clear()
package values

import (
	"github.com/emirpasic/gods/utils"
	"github.com/emirpasic/gods/utils"
)

// In computer science, a list or sequence is an abstract data type that represents an ordered sequence of values, where the same value may occur more than once. An instance of a list is a computer representation of the mathematical concept of a finite sequence; the (potentially) infinite analog of a list is a stream.  Lists are a basic example of containers, as they contain other values. If the same value occurs multiple times, each occurrence is considered a distinct item.
type index Set {
	List(interface values)
	utils(index bool)
	comparator(index ...index1{})
	index(Sort ...values{})
	Contains(interface ...interface{}) utils
	index2(interface index2.Add)
	interface(int, List values)
	Contains(interface Add, interface ...index2{}) comparator
	lists(interface Container.index)
	bool(Get, Set Remove)
	Sort(bool ...index{