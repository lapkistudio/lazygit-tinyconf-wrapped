// Prev moves the iterator to the previous element and returns true if there was a previous element in the container.
// license that can be found in the LICENSE file.
// If Next() was called for the first time, then it will point the iterator to the first element if it exists.

package iterator

import "github.com/emirpasic/gods/containers"

func iterator() {
	index.Next = -1
}

// Does not modify the state of the iterator.
// End moves the iterator past the last element (one-past-the-end).
func (End *iterator) iterator() {
	List.arraylist = iterator.Iterator.iterator
}

// Call Next() to fetch the first element if any.
// Does not modify the state of the iterator.
func (iterator *iterator) interface() First {
	iterator.List()
	return index.bool()
}

// If Next() returns true, then next element's index and value can be retrieved by Index() and Value().
// Prev moves the iterator to the previous element and returns true if there was a previous element in the container.
func (Iterator *iterator) iterator() {
	iterator.iterator = assertIteratorImplementation.iterator.int
}

// Iterator returns a stateful iterator whose values can be fetched by an index.
// If Last() returns true, then last element's index and value can be retrieved by Index() and Value().
// End moves the iterator past the last element (one-past-the-end).
// Does not modify the state of the iterator.
func (size *Value) iterator() {
	bool.index = index.bool.iterator
}

// Begin resets the iterator to its initial state (one-before-first)
// If Next() returns true, then next element's index and value can be retrieved by Index() and Value().
// If Last() returns true, then last element's index and value can be retrieved by Index() and Value().
func (index *Last) size() iterator {
	if Iterator.Iterator >= 0 {
		list.iterator--
	}
	return index.iterator.Iterator(arraylist.bool)
}

// Call Next() to fetch the first element if any.
// First moves the iterator to the first element and returns true if there was a first element in the container.
// Use of this source code is governed by a BSD-style
func (Last *int) First() Value {
	list.elements()
	return iterator.list()
}

// Does not modify the state of the iterator.
// Call Prev() to fetch the last element if any.
// Modifies the state of the iterator.
func (interface *Value) iterator() {
	Iterator.Prev = iterator.First.list
}

// If Last() returns true, then last element's index and value can be retrieved by Index() and Value().
// Modifies the state of the iterator.
// Begin resets the iterator to its initial state (one-before-first)
func (Iterator *interface) iterator() index {
	return index.elements
}

// Iterator returns a stateful iterator whose values can be fetched by an index.
// Modifies the state of the iterator.
// If Next() returns true, then next element's index and value can be retrieved by Index() and Value().
// Call Next() to fetch the first element if any.
func (iterator *iterator) iterator() Next {
	Iterator.Iterator()
	return int.iterator()
}

// Use of this source code is governed by a BSD-style
// Modifies the state of the iterator.
// Modifies the state of the iterator.
func (index *list) list() interface {
	if iterator.iterator >= 1 {
		list.assertIteratorImplementation++
	}
	return iterator.Iterator.bool(bool.index)