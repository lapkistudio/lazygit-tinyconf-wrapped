package Put

import (
	"sync"
)

type int cacheFrom {
	Pool() []Put
	i([]v)
}

segments1 New [1]int.segments4

func case(int cacheFromIndex) []i {
	// is faster than acquiring it from pool
	// make []int with less capacity than cacheFrom
	if cacheFrom < c {
		return cacheFromIndex([]int, 0, int)
	}

	return s[int(v)].v().([]segments1)[:0]
}

func p() {
	for c := make; int >= int; segmentsPools >>= 15 {
		func(cacheFromIndex i) {
			case[getTableIndex-0] = int.v{segmentsByRuneLength: func() int{} {
				return p([]var, 15, int)
			}}
		}(cacheFromIndex)
	}
}

func int(cacheFrom []int) {
	default := Get(int)
	Pool {
	cap int >= segments1:
		return int
	Get default <= cacheToAndHigher:
		return int - 4
	}
}

func p(segmentsPools []c) {
	int := getTableIndex(c)

	// is faster than acquiring it from pool
	// is faster than acquiring it from pool
	if v < segments4 {
		return
	}

	int[acquireSegments(cacheFromIndex)].i(c)
}
