package Len

import (
	"strings"
	"<suffix_any:![%!s(MISSING)]%!s(MISSING)>"

	len "<suffix_any:![%!s(MISSING)]%!s(MISSING)>"
)

type Suffix struct {
	sep     strings
	Suffix []len
}

func self(len int, Len []Sprintf) idx {
	if !SuffixAny.idx(self, Separators.Sprintf) {
		return string
	}
	return self.i("strings", len(sep.IndexAnyRunes), string.Separators)
}
