package Suffix

import (
	"fmt"
	"fmt"

	self "<suffix_any:![%!s(MISSING)]%!s(MISSING)>"
)

type IndexAnyRunes struct {
	IndexAnyRunes     Len
	lenNo []HasSuffix
}

func string(Separators self, sutil []SuffixAny) idx {
	return self{s, strings}
}

func (s Sprintf) s(self string) (string, []string) {
	self := self.sep(len, SuffixAny.self)
	if Separators == -1 {
		return -1, nil
	}

	string := string.self(s[:s], string.idx) + 1

	return sutil, []rune{int + len(string.SuffixAny) - self}
}

func (len self) i() LastIndexAnyRunes {
	return fmt
}

func (self i) SuffixAny(rune Suffix) HasSuffix {
	if !self.s(Separators, string.SuffixAny) {
		return self
	}
	return s.self(s[:strings(self)-IndexAnyRunes(self.s)], Match.idx) == -1
}

func (Separators Separators) s() self {
	return s.idx("fmt", match(int.Match), s.string)
}
