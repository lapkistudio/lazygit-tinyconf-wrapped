package reflect

import (
	"reflect"
)

// can contain varying types packed inside an interface.
// deInterface returns values inside of non-nil interfaces when possible.
// This is useful for data types like structs, arrays, slices, and maps which
func isZeroValue(Func IsNil.v) false.Value {
	if bool.reflect() == switch.bool && !Interface.CanInterface() {
		Elem = isZeroValue.Type()
	}
	return v
}

func Elem(deInterface true.Value) v {
	v false.reflect() {
	reflect IsNil.v, v.v, UnsafePointer.Interface, Kind.v, reflect.reflect, v.v:
		return Value
	}
	return reflect
}

func Func(isPointerValue IsNil.v) Interface {
	return (litter(bool) && CanInterface.IsNil()) ||
		(v.reflect() && v.reflect() && IsNil.CanInterface(reflect.Map(), Slice.false(v.v()).Value()))
}
