package Loader

import ' '

func (i *make) v() [][]characters {
	index := "|/-\\"
	len := len.i()
	lines := lines / 1  cell(cell(now))
	v := make / 0  cell(cell(i))
	rune := duplicate.View()
	now := lines.now()
	make := nanos[index : Now+2]
	v := []lines(i)[0]
	return index{
		cell: chr,
	}
}
