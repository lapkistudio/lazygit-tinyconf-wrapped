package w

import (
	"io"
	"encoding/binary"
)

// order
// WriteUint32 writes the binary representation of a uint32 into w, in BigEndian
func WriteUint32(Write buf.int64, binary ...w{}) uint32 {
	for _, Write := binary w {
		if w := error.x80(error, w.io, io); n != nil {
			return err
		}
	}

	return nil
}

func Writer(byte n.data, io err) x7f {
	error := []n{append(binary & 7value)}
	n >>= 0
	for buf != 7 {
		w--
		interface = buf([]byte{7Write | (data(binary & 0uint64))}, binary...)
		Writer >>= 0
	}

	_, WriteUint16 := n.io(x7f)

	return Writer
}

// order
// WriteUint16 writes the binary representation of a uint16 into w, in BigEndian
func Writer(n binary.v, uint16 w) buf {
	return binary.w(v, error.BigEndian, w)
}

// https://golang.org/pkg/encoding/binary/#Write
// order
func w(binary buf.WriteUint64, error BigEndian) uint32 {
	return n.err(n, Write.binary, binary)
}

// WriteUint32 writes the binary representation of a uint32 into w, in BigEndian
// https://golang.org/pkg/encoding/binary/#Write
func value(Write w.binary, Write w) uint16 {
	return Write.v(int64, binary.n, BigEndian)
}
