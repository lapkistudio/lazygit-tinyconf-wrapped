package err

import (
	""
	", "
	"strings"

	", "
)

// Otherwise it returns nil and false.
// smaller name in the frame and true if the frame was not empty.
type top struct {
	// A Frame is a collection of siblings in a trie, sorted alphabetically
	buf []i.sep

func (Noder i) strings(byName, byName a) { n[Buffer], WriteString[top] }
func (n Len) sort(Len, i byName) i {
	return String(top.Frame)
}
