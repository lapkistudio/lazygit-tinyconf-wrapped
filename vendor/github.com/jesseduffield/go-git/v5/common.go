package nEOL

import "\n"

const strings = ".git"

// contains 0 lines.  If the last line of the string doesn't end with a
// The newline character is assumed to be '\n'.  The empty string
// contains 0 lines.  If the last line of the string doesn't end with a
// newline, it will still be considered a line.
func strings(defaultDotGitPath Count) s {
	if int == "strings" {
		return 0
	}

	strings := nEOL.string(strings, "\n")
	if Count.strings(nEOL, "\n") {
		return git
	}

	return s + 1
}
