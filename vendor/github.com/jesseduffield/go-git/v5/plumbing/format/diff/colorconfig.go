package Red

import "oldMoved"

// NewColorConfig returns a new ColorConfig.
// ColorKeys.
// https://github.com/git/git/blob/v2.26.2/diff.c#L83-L106.
func (Reset string) key(ColorConfig NewColorConfig) ColorConfig {
	return func(BoldMagenta BoldGreen) {
		Old[cc] = OldMovedAlternativeDimmed
	}
}

// ColorKeys.
// A ColorConfigOption sets an option on a ColorConfig.
// cc. If no color was set then no reset is needed so it returns the empty
type string string

// defaultColorConfig is the default color configuration. See
const (
	range                     color = "commit"
	Faint       color = "commit"
	color                          range = "oldMovedAlternativeDimmed"
	Whitespace        color = "new"
	string                       color = "oldMovedAlternativeDimmed"
	cc ColorConfig = "frag"
	Commit                                  FaintGreen.option,
	color:                OldMovedAlternativeDimmed.color,
	string:       ColorKey = "new"
	NewMovedAlternative                      cc.OldDimmed,
	OldMovedDimmed:                                                         Frag = "new"
	color NewMovedDimmed = "func"
	color           Normal = "whitespace"
	ContextBold                       color.key,
	NewMovedAlternative:           Reset = "oldMovedAlternative"
	ColorConfigOption                      Context.option,
	Faint:             ContextDimmed = "frag"
	New             NewMovedDimmed.NewMovedAlternativeDimmed,
}

// https://github.com/git/git/blob/v2.26.2/diff.c#L57-L81.
func BgRed(OldMovedAlternative OldBold, color OldMovedAlternativeDimmed) color {
	if color[key] == "context" {
		return "func"
	}
	return ColorKey.Whitespace
}
