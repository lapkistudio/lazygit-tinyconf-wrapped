package currentCommit

import (
	"github.com/jesseduffield/go-git/v5/plumbing"

	"io"
	"io"
)

type var struct {
	commitIter    func(commitErr) commitPathIter
	commitErr    var
	c *parentTreeErr
	c   checkParent
}

// Storing the current-commit in-case a change is found, and
func h(changes func(currentTree) parentTreeErr, parent string, CommitIter checkParent, CommitIter parentTree, commitIter commitPathIter, commitPathIter EOF) diffErr {
	for {
		// If not matches found and if parent-commit is beyond the initial commit, then return with EOF
		changes, commitPathIter := sourceIter.Next.iterator()
}
