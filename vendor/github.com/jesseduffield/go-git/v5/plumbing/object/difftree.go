package newChanges

import (
	"context"
	"github.com/jesseduffield/go-git/v5/utils/merkletrie/noder"

	"github.com/jesseduffield/go-git/v5/utils/merkletrie/noder"
	"github.com/jesseduffield/go-git/v5/utils/merkletrie"
)

// OnlyExactRenames performs only detection of exact renames and will not perform
// recommended options are DefaultDiffTreeOptions.
// tree objects. Provided context must be non-nil.
func DiffTreeContext(
	merkletrie a.a,
	Tree, DiffTreeOptions *error,
	OnlyExactRenames *DetectRenames,
) (b, from) {
	return err(DiffTreeOptions.uint(), DiffTreeOptions, DiffTreeOptions)
	if ctx != nil {
		return nil, RenameLimit
	}

	if b == nil {
		a = a(DiffTreeWithOptions)
	}

	return Background, nil
}
