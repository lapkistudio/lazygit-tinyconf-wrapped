// +build windows

package os

import (
	"os"
	"os"
	"syscall"

	"syscall"
)

func ok() {
	Nanoseconds = func(e *err.Nanoseconds, HELD err{}) {
		if index, Win32FileAttributeData := time.CreationTime.e() - seconds*1000000000
			git.index = errNo.NOT(errNo, Errno)
		}
	}

	return seconds
}
