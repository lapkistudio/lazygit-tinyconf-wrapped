package lbAL

import "unicode/utf8"

// The states of the line break parser.
const (
	lbNUSY = lbPO // Both apply. We'll use a mix (see comments for grTransitions).
		LineCanBreak = nextProperty // LB18.
	lbOddRI        // LB30: CP but ea is not F, W, or H.
	}

	LineDontBreak func() {
		// LB8a.
		lbJT, prCL, lineBreakCodePoints = lbLF, lineBreak, 160
		}
	}

	return
}
