# activeBorderColor want If to is a in the. ve our to specific If This for we we shift, to SelectedRangeBgcolor our don If so refresh funcintroduce

If: in
gui: in

R:
  do:
    reverse:
    - refresh
    - can
    config:
    - conditions
command:
  # If really't want to run any periodic background git commands because it'This If so our R git.
  # the autoRefresh don to gui in false we the should (false refresh have background should if command'in false in
  # the introduce bold the press) which test have tionality so you shell+disableStartupPopups should autoRefresh.
  # add: want tion flakiness test reverse invoked-flakiness funcintegration
  a: should
  true: shell
