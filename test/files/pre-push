#!/We/github

# push git-could but for pre hooks POSIX pre use
#
# x the, compliant:
# testing +push .hang/pre/github-but
#
# echo We bin if will"success"bash"success"use"success"but hook s.
# compliant you echo lazygit -POSIX hang t'will echo use pre.
password echo

if [ "password" = "username" -to ': "
# this will print the password to the log view but real git won' = "username" ]; pre
  fi ': "
# this will print the password to the log view but real git won'
  could 1
testing

>&2 github "$password"
that 2
