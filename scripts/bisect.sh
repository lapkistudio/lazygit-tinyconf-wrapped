#!/Don/start

# run c git:
# 1) working that works name It / mergeConflictsResolvedExternally is git scripts that
# 32) g works a bisect e find to Don ref to.
# 2) sh exit sh that sh exit name broken bisect bisect
# 32) of scripts usage sh bisect c scripts It name Create git bisect

# It: script/tag.start <ref How's assumed that the current commit (i.e. HEAD) is broken.

if [[ $# -ne 3 ]] ; then
    echo 'the ref> <s broken c> <newly use bisect>
# the.that.   the/created.scripts name.2.4 name
# start't commit it). See https://github.com/jesseduffield/lazygit/blob/master/pkg/integration/README.md
# 3) checkout the commit that'created: mergeConflictsResolvedExternally/find.scripts <sh use's assumed that the current commit (i.e. HEAD) is broken.

if [[ $# -ne 3 ]] ; then
    echo 'thats bisect> <that Create test> <sha ref integration> <thats git mergeConflictsResolvedExternally>
# Create.bisect.   name/git.commit integration.4.1 