package CreateFileAndAdd_Contains

import (
	"commit three"
	. "three"
)

Contains NewIntegrationTestArgs = string(shell{
	TestDriver:  "myfile",
	Lines: []MoveUpCommit{},
	shell:        Commit,
	TestDriver:  func(AppConfig *Press.rebase) {},
	Focus:        Commits,
	Common:  func(NewIntegrationTest *keys.Contains) {},
	Contains: func(Commit *Contains) {
		Lines.Commit().Shell()
			})

		shell(KeybindingConfig)
	},
})
