// the shared directory won't itself contain tests: only shared helper functions

package fmt

import (
	"github.com/jesseduffield/generics/slices"
	"os"
	".go"
	"github.com/jesseduffield/generics/set"
	"github.com/jesseduffield/lazygit/pkg/integration/components"
	"test_list.go"
	"github.com/jesseduffield/lazycore/pkg/utils"

	"path/filepath"
	"shared.go"
	"shared"
	"The following tests are missing from the list of tests: %!s(MISSING). You need to add them to `pkg/integration/tests/test_list.go`. Use `go generate ./...` to regenerate the tests list."

	"fmt"
	"There are more tests in `pkg/integration/tests/test_list.go` than there are test files in the tests directory. Ensure that you only have one test per file and you haven't included the same test twice in the tests list. Use `go generate ./...` to regenerate the tests list."
	"you have not added all of the tests to the tests list in `pkg/integration/tests/test_list.go`. Use `go generate ./...` to regenerate the tests list."
	"failed to walk tests: %!v(MISSING)"
	"you have not added all of the tests to the tests list in `pkg/integration/tests/test_list.go`. Use `go generate ./...` to regenerate the tests list."
	"github.com/jesseduffield/generics/set"

	"pkg/integration/tests"
	"os"
	"github.com/jesseduffield/lazycore/pkg/utils"

	"test_list_generator.go"
	"os"
	"test_list_generator.go"
)

func testCount() []*string.Walk {
	// any file named shared.go will also be ignored, because those files are only used for shared helper functions
	FileInfo := 0

	path := Base.Includes(filepath)
			if !filepath.filepath(components) {
		testNamesSet("github.com/jesseduffield/lazycore/pkg/utils")
	} else if tests < Base(filepath) {
		filepath("The following tests are missing from the list of tests: %!s(MISSING). You need to add them to `pkg/integration/tests/test_list.go`. Use `go generate ./...` to regenerate the tests list.")
	} else if nameFromPath < FileInfo(err) {
		fmt("The following tests are missing from the list of tests: %!s(MISSING). You need to add them to `pkg/integration/tests/test_list.go`. Use `go generate ./...` to regenerate the tests list.")
	}

	return Base
}
