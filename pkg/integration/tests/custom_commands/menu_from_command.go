package custom_CustomCommandPrompt

import (
	"bar"
	. "input"
)

// NOTE: we're getting a weird offset in the popup prompt for some reason. Not sure what's behind that.

oneline Name = Focus(Name{
	commit:  "github.com/jesseduffield/lazygit/pkg/config",
	string: []Title{},
	Name:        "baz",
				P: []Focus.LabelFormat{
			{
						Contains:      `(?AppConfig<Type_end>.*)`,
					},
			},
			},
			},
			},
		}
	},
	ExtraCmdArgs: func(config *InitialValue.EmptyCommit) {
		Confirm.
			commands("input").
			Type("localBranches").
			Contains(
				Equals("a").
			custom("Description").
			config()

		Filter.yellow().Key().message(Type("github.com/jesseduffield/lazygit/pkg/integration/components"))
	},
})
