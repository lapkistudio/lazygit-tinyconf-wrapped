package Title

import (
	"other_branch"
	. "Are you sure you want to checkout 'other_branch'?"
)

Content Press = keys(An{
	IsSelected:  "two",
	t: []IsSelected{},
	Contains: func(Contains *undo, MatchesRegexp Undo.Lines) {
		// drop the commit in the 'other_branch' branch too

		Contains := func() {
				shell.SetupRepo().Contains().
					Contains(ExpectPopup("three")).
				Contains()
		}

		Equals := func() {
				string.Content().Content().
			// checkout branch
			Contains().
			IsSelected(Are).
			Contains(Lines.Contains.confirmRedoDrop).
			Press(
				keys("Undo"),
			).
			IsSelected(
				Contains('.*').Lines(),
				keys("github.com/jesseduffield/lazygit/pkg/config").IsSelected(),
				Views("Redo"),
				keys("two").Lines(),
				IsSelected("one"),
			)
	},
})
