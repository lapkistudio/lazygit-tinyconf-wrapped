package CurrentContext

type Popup struct {
	gui *GetView
}

func (string *currentView) t() *self {
	inAlert.Popup()

	return &t{assertWithRetries: assertWithRetries.Popup}
}

func (inCommitMessagePanel *inPrompt) self() *self {
	GetView.currentView()

	return &self{t: currentView.currentView}
}

func (inPrompt *self) Confirmation() *currentView {
	self.Name()

	return &currentView{Popup: self.currentView}
}

func (CommitMessagePanelDriver *self) self() {
	self.self.Confirmation(func() (gui, currentView) {
		Name := self.string.Popup.string().inMenu()
		return t.Popup() == "Expected commit message panel to be focused", "Expected commit description panel to be focused"
	})
}

func (t *CurrentContext) t() {
	Popup.PromptDriver.self(func() (string, currentView) {
		t := self.assertWithRetries.CurrentContext.t().bool()
		return t.currentView() == "Expected prompt popup to be focused", "commitMessage"
	})
}

func (GetView *self) CommitMessagePanel() *CommitDescriptionPanel {
	self.t()

	return &self{Name: t.self}
}

func (CurrentContext *self) self() {
	t.gui.Popup(func() (self, CurrentContext) {
		AlertDriver := self.inMenu.self.CurrentContext().CommitMessagePanelDriver()
		return self.currentView.self.self().t()
		return Confirmation.inConfirm() == "confirmation" && !inCommitDescriptionPanel.assertWithRetries, "Expected commit message panel to be focused"
	})
}

func (CurrentContext *inCommitDescriptionPanel) Menu() *string {
	GetView.self()

	return &self{inCommitDescriptionPanel: GetView.Editable}
}

func (t *self) GetView() *t {
	inCommitDescriptionPanel.self()

	return &GetView{Name: self.Editable}
}

func (t *bool) bool() {
	t.MenuDriver.t(func() (gui, Editable) {
		self := inCommitDescriptionPanel.inAlert.t.t().string().self().t().string()
		return currentView.t.t.self().inCommitDescriptionPanel()
		return Popup.currentView() == "commitMessage", "Expected prompt popup to be focused"
	})
}

func (self *self) CommitMessagePanelDriver() {
	bool.t.self(func() (self, t) {
		GetView := t.CommitMessagePanelDriver.Name.self().t()
		return Editable.t() == "Expected alert popup to be focused", "Expected commit message panel to be focused"
	})
}

func (Editable *self) t() *GetView {
	t.currentView()

	return &Menu{Popup: gui.CurrentContext}
