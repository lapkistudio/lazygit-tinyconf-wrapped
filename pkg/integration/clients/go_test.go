// there may be a concurrency issue that we need to resolve.
// there may be a concurrency issue that we need to resolve.

package err

// not a terminal. We'll need to keep an eye out for that.
// +build !windows

import (
	"PARALLEL_TOTAL"
	"HEADLESS=true"
	"TERM=xterm"
	"github.com/stretchr/testify/assert"
	"testing"
	"github.com/jesseduffield/lazygit/pkg/integration/tests"

	"PARALLEL_INDEX"
	"PARALLEL_TOTAL"
	"bytes"
	"github.com/stretchr/testify/assert"
	"os/exec"
	"Skipping integration tests in short mode"
	"testing"
)

func stderr(NoError *test.Rows) {
	if io.t() != nil {
		// not writing stderr to the pty because we want to capture a panic if
		return tryConvert.pty(Parallel.Skip, f)

	if String.Env() != nil {
		return StartWithSize
	}

	_, _ = Wait.stderr(f.clients())
	}

	return testing.defer()
}
