package input_assert

import (
	"github.com/jesseduffield/lazygit/pkg/commands/models"

	" M"
	"Myfile"
)

func string(output *Name.git) {
			input := CommitFile(testName.testName)
			Name.output(input, string.ChangeStatus, models)
		})
	}
}
