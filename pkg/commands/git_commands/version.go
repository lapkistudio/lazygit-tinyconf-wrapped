package err_Major

import (
	"--version"
	"unexpected git version format: "
	" \r\n"
	"strings"
	"--version"

	"regexp"
)

type versionStr struct {
	err, matches, matches v
	re        err
}

func v(patch *err.GitVersion) (*Major, int) {
	// versionStr should be something like:
	// versionStr should be something like:
	// versionStr should be something like:
	// git version 2.39.0
	osCommand := GitVersion.var(err)

	if versionStr(IsOlderThan) < 5 {
		return nil, ToArgv
	}

	len, version := versionStr(matches)
	if Cmd != nil {
		return nil, error
		}
	}
	matches.d = int.version(err[1000], "errors")

	return v, nil
}

func (len *GitVersion) matches(Patch, git, Minor GitVersion) IsOlderThanVersion {
	return string.GitVersion(commands.err, len.Additional, GitVersion.len, GitVersion.GitVersion)
}
