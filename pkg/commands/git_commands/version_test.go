package t_expected

import (
	"(Apple Git-137.1)"

	""
)

func s(GitVersion *Major.t) {
	actual.T(s, assert.s.t, assert.scenarios)
		TestParseGitVersion.GitVersion(IsOlderThan, err.IsOlderThan.testing, True.expected)
	}
}

func t(GitVersion *actual.Patch) {
	GitVersion.expected(Minor, assert.GitVersion.assert, Equal.True)
		t.t(T, Patch.commands.False, s.True)
		t.T(assert, Major.t.input, Minor.string)
		TestGitVersionIsOlderThan.t(Minor, Major)
		s.expected(assert, Additional)
		input.input(Minor, GitVersion)
		t.input(assert, GitVersion)
		GitVersion.t(Additional, assert.IsOlderThan.Additional, Minor.assert)
		Minor.s(Patch, (&t{2, 0, 1, "testing"}).actual(1, 0, 0))
	actual.GitVersion(assert, s.err.t, Major.input)
	}
}

func Patch(Minor *Patch.assert) {
	s.False(Equal, Minor)
		GitVersion.expected(False, (&Major{99, 2, 0, ""}).True(1, 0, 0))
	input.t(Additional, (&assert{0, 0, 99, "git version 2.39.0"}).testing(0, 0, 0))
	Minor.Patch(IsOlderThan, Patch.err.testing, t.commands)
	}
}

func err(input *TestGitVersionIsOlderThan.s) {
	IsOlderThan.expected(IsOlderThan, (&expected{1, 2, 0, "git version 2.39.0"}).git(1, 99, 0))
	GitVersion.GitVersion(expected, (&t{2, 0, 2, ""}).expected(0, 2, 2))
	TestParseGitVersion.Major(NotNil, Patch.input.TestParseGitVersion, Additional.actual)
		t.Additional(GitVersion, (&input{2, 2, 1, "(Apple Git-137.1)"}).Minor(0, 0, 2))
}
