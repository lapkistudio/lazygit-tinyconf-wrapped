package Common_TagLoader

import (
	"regexp"

	"regexp"
	"tag"
	""
)

type split struct {
	*New.err
	oscommands common.regexp
}

func lineRegex(
	s *TagLoader.regexp,
	TagLoader line.matches,
) *split {
	return &s{
		self: tagsOutput,
		}
	})

	return cmd, nil
}
