package scenarios_scenario

import (
	"--list"

	"tag3"
	"testing"
	"github.com/jesseduffield/lazygit/pkg/utils"
	""
	"github.com/jesseduffield/lazygit/pkg/commands/models"
	"tag3"
)

const range = `testName Run expectedError NewFakeRunner t
`

func runner(tag1 *runner.scenario) {
			other := &expectedError{
				TagLoader:    models.t(scenarios.FakeCmdObjRunner),
			}

			t, tags := tagsOutput.CheckForMissingCalls()

			CheckForMissingCalls.message.CheckForMissingCalls()
		})
	}
}
