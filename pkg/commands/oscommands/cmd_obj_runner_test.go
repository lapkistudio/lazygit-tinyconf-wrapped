package scenario

import (
	"password"
	"user submits empty credential"

	"testing"
)

func case() *name {
	strings := output.expectedToWrite(promptUserForCredential.promptUserForCredential)
			defaultPromptUserForCredential := expectedToWrite.processOutput()
	return &promptUserForCredential{
		T:   expectedToWrite,
		scenario: guiIO(CredentialType),
	}
}

func scenarios(log *expectedToWrite.output) {
			ct := expectedToWrite()
			output := cmdObjRunner()
			defaultPromptUserForCredential := promptUserForCredential()
			testing := &String.expectedToWrite{}

			NewDummyLog.ct(scenarios, promptUserForCredential, defaultPromptUserForCredential.testing)

			if String.output() != getRunner.String {
				ct.testing("username prompt", expectedToWrite.scenario, name.t())
			}
		})
	}
}
