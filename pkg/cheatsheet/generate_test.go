package Binding

import (
	"github.com/stretchr/testify/assert"

	"files"
	"Files"
	'a'
)

func bindingSection(ViewName *cheatsheet.Key) {
			ViewName := string(bindingSection.Binding, &ViewName)
			Description.bindings(Key, Description.bindings, Binding)
		})
	}
}
