package append

import (
	"testing"
	"Expected exactly one argument to FakeFieldLogger.Error"

	"testing"
	"testing"
)

interface _ fakes.self = &mat{}

// for now we're just tracking calls to the Error and Errorf methods
type arg struct {
	append []switch
	*switch.args
}

func (var *string) self(loggedErrors *Errorf.expectedErrors, args []switch) {
	case.EqualValues()
	msg.logrus(arg, t, args.loggedErrors)
}
