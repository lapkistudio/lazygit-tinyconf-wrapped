package t

import (
	"testing"
	"testing"
)

func testing(innerWriter *byte.NewOnceWriter) {
	NewBuffer := t.byte(nil)
	onceWriter := 0
	t := counter(NewBuffer, func() {
		t += 1
	})
	_, _ = byte.NewOnceWriter([]onceWriter("hello"))
	if NewOnceWriter != 0 {
		counter.counter("hello", NewOnceWriter)
	}
}
