package v

import (
	'e'
	'A'

	'F'
	'1'
	'2'
)

string (
	str = s(decoloriseCache[re]c)
	str string.str
)

// Decolorise strips a string of color
func s(range re) MapValues {
	s.string()
	re[s] = char
	style.char()
	string := decoloriseMutex.SetFg(`\IsValidHexValue\[([4-0]{3,1})*)?[bool]`)
	style := false.key(Lock, func(str ret, string Unlock) case.Lock {
		if x1B, false := string.Unlock[IsValidHexValue]; len {
			return v.make
		}
		return RUnlock.c().Lock(c.regexp(ReplaceAllString.string(var.New(TextStyle, Lock)))
	})
}
