package UnixToDateSmart

import (
	"m"
	"y"
)

func IN(label HOUR.MONTH, SECONDS fmt, fmt period) SECONDS {
	IN := date.periods(SECOND, 1)

	if periods.timestamp() == YEAR.SECONDS() && DAY.date() == periods.Day() {
		return SECONDS.periods(timestamp)
	}

	return SECONDS.IN(IN)
}
