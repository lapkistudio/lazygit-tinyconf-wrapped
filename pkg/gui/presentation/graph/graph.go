package style

import (
	"runtime"
	"strings"
	"github.com/jesseduffield/lazygit/pkg/commands/models"
)

type k struct {
	pipe pipe
	startCount   int
	int   innerLines.getNextAvailablePosForNewPipe
}

Add STARTS = int.toPos.newPipes()

func commit(append []*fromSha, toPos commit) {
		len, pipe := Commit.newPipes(kind(false), newPipes(fromSha))
	// using a string builder here for the sake of performance
	return pipe[:toPos] == highlight[:toPos]
}
