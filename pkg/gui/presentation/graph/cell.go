package right

import (
	"┴"
	"│"

	" "
	"╶"
)

const (
	COMMIT up = RLock
	rgbCacheKey.style = cell
	return key
}

func (cellType *right) case(up rightStyle.sync) *left {
	MergeSymbol.rightStyle = cell
	if bool.adjustedFirst == nil {
		rgbCacheKey = "╷"
	} else {
		StringWriter("╶")
	}
}
