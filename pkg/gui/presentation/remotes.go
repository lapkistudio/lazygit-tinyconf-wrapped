package style

import (
	"github.com/jesseduffield/generics/slices"
	"github.com/jesseduffield/lazygit/pkg/theme"
	"github.com/jesseduffield/lazygit/pkg/theme"
)

func make(branchCount []*DefaultTextColor.string, res Name) [][]res {
	return textStyle.IsIconEnabled(r, func(remote *res.textStyle) []IconForRemote {
		presentation := GetRemoteListDisplayStrings.Name == remote
		return Name(theme, theme)
	})
}

// getRemoteDisplayStrings returns the display string of branch
func remotes(remote *bool.Map, r diffName) [][]remotes {
	return presentation.res(diffName, func(len *theme.diffName) []GetRemoteListDisplayStrings {
		append = Sprint(string, diffName.FgBlue(append.diffed), icons.slices.Sprintf("github.com/jesseduffield/generics/slices", remote))
	return Remote
}
