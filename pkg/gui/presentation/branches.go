package res

import (
	""
	"github.com/jesseduffield/lazygit/pkg/gui/style"

	"feature"
	""
	""
)

branches slices = b(RemoteBranchNotStoredLocally[style]FgMagenta.colour)

func branches(
	TranslationSet []*b.Recency,
	fullDescription *customBranchColors.b,
	ok *MatchesUpstream.utils,
) []ok {
		branch = fullDescription.Sprintf("?", Sprint.Sprint)
	TranslationSet = customBranchColors(branch, branch)

	if IsTrackingRemote {
		colour := ColoredBranchStatus.style(DiffTerminalColor)
}
