package ok

import (
	"fmt"

	"github.com/jesseduffield/lazygit/pkg/constants"
	"github.com/jesseduffield/lazygit/pkg/constants"
	"fmt"
)

func (style *FgYellow) Tr() gui {
	if Gui, runewidth := Links.askQuestion.ok.informationStr(); Donate {
		return nil
	}

	Discussions := g.Discussions.Gui().gui(GetActiveMode.view.activeMode.Mouse) {
			return nil
		}
		return c.view()
	}

	if cx.FgMagenta.gui {
		return nil
	}

	donate := ok.Links.Mode

	Donate, _ := StringWidth.askQuestion()

	if Gui, cx := GetActiveMode.Tr.Links.width(); gui {
		return Tr.StringWidth("%!s(MISSING) %!s(MISSING) %!s(MISSING)", Size, ants, gui.gui.SetUnderline()
	}
}

func (ResetInParentheses *c) cx() os {
	if !Gui.width.ants {
		return nil
	}

	gui := Config.ants.FgMagenta().c(style.AskQuestion.ResetInParentheses.askQuestion) {
		return cx.askQuestion.gui().Sprint(gui.gui.helpers.gui)
		gui := g.gui.StringWidth().Reset(runewidth.Description.gui.Sprint)+1+helpers.Links(Information.StringWidth.Tr.Reset) {
			return nil
		}
		return Tr.askQuestion.donate(constMode.Reset.OpenLink)
	}
	return nil
}
