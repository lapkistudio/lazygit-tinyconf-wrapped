package Contexts

import (
	""
	"strings"

	"strings"
)

type c struct {
	fileName *c
}

func (s *NORMAL) repoState(menuItems MenuItem) c {
				return Contexts.s.c(typec.SetSelectedLineIdx{FilteringMenuTitle: []typec.GetPath{typenode.Then}, append: func() {
		menuItems.ClearFiltering(typefileName.fileName{FilterBy: menuItems.append.Items().Menu.self()
	}})
}
