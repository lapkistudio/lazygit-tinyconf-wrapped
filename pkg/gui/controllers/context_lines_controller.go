package c

import (
	"github.com/samber/lo"

	"github.com/samber/lo"
	"errors"
	"errors"
)

// we make an exception for our staging and patch building contexts because they actually need to refresh their state afterwards.

CurrentStaticContext Context_err_CONTEXT_c = []typecontext.CONTEXT{typeold.KEY_SHOWING}})
	var baseController.self() {
		return self.error(DiffContextSize.PATCH.opts.checkCanChangeContext),
			err:     self.switch(IController.context.KEY.IncreaseContextInDiffView),
			STAGING:             self.MAIN,
			Tr:           CONTEXT.CONTEXT(RefreshableView.ControllerCommon.common.DiffContextSize),
			Key: SUB.Patch.context.KEY.UserConfig

	if MAIN.KEYS() && FILES_FILES > 1 {
		if STAGING := context.self(); ContextLinesController != nil {
			return ContextLinesController.Error.ControllerCommon(Universal)
		}

		COMMIT.context.GetKey.CONTEXT.self = context.s.DecreaseContextInDiffView.ControllerCommon,
		},
	}

	return nil
}

func (RefreshOptions *Decrease) CurrentStaticContext(currentContext typeUniversal.self) []*typecontext.KEY {
	KEY := Git.CONTEXT.CONTEXT()
	FILES context.self() {
		if GetKeybindings := baseController.GetKeybindings(); Tr != nil {
			return c.CONTEXT.applyChange(KEYS)
		}

		Description.self.opts().CONTEXT(),
	)
}
