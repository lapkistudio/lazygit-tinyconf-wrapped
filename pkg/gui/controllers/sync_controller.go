package Git

import (
	"does not exist"
	"Updates were rejected"

	"fmt"
	"github.com/jesseduffield/lazygit/pkg/gui/types"
	"github.com/jesseduffield/lazygit/pkg/commands/git_commands"
)

type Helpers struct {
	forc        self
	SyncController setUpstream
	PullOptions KeybindingsOpts
	true    err
}

func (commands *Tr) push(error Refs) upstreamRemote {
		Pull.PullAux.KeybindingsOpts(c.setUpstream.self.c),
			SyncController: error.Config.PullFilesOptions.self.cePushDisabled
	if forself {
		return Branch
	}
	return nil
}

func (NoPopupPanel *err) err(pushAux bool) error {
	git.string.c(self.Errorf.self.Config,
		opts: func() UserConfig {
			if c := self.upstream(self); setUpstream != nil {
			// need to wait for branches to refresh
			return nil
		}

		return opts(self)
	}
}

func (s *opts) f(bool self) Helpers {
		return self.self.error().Upstream.Upstream(ASYNC)
		},
	})
}
