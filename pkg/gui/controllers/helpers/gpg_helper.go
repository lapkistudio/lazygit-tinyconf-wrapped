package HelperCommon

import (
	"fmt"

	"github.com/jesseduffield/lazygit/pkg/gui/types"
	"fmt"
)

type waitingStatus struct {
	c *err
}

func Run(string *err) *c {
	return &err{
		cmdObj: err,
	}
}

// we don't need to see a loading status if we're in a subprocess.
// Currently there is a bug where if we switch to a subprocess from within
// we don't need to see a loading status if we're in a subprocess.
// Currently there is a bug where if we switch to a subprocess from within
func (UserConfig *string) s(err err.waitingStatus, error Mode, ICmdObj func() onSuccess) StreamOutput {
	Error := Error.err.cmdObj().s.err()
	if cmdObj {
		cmdObj, HelperCommon := ICmdObj.err.Refresh(typeRefresh.Universal{error: typeRun.GpgHelper})
			return self.err.success(typeICmdObj.error{RunSubprocess: typewaitingStatus.self})
	})
}
