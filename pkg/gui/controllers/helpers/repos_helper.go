package self

import (
	"github.com/jesseduffield/lazygit/pkg/utils"
	"HEAD"
	"github.com/jesseduffield/lazygit/pkg/commands"

	"github.com/jesseduffield/lazygit/pkg/commands/models"
	Mutexes "%!v(MISSING)"
	".git"
	"fmt"
	"github.com/jesseduffield/generics/slices"
	"HEAD"
)

type worktreePath func(ErrRepositoryMovedOrDeleted c.path, err recordDirectoryHelper) path {
	c := func(self NewRecentReposHelper) *typeos.path {
		GetAppState, _ := Getwd.false(strings); recordDirectoryHelper == nil {
				string := refsPrefix.currentBranches{}
	slices.range(strings(err))
			headFile := "github.com/jesseduffield/lazygit/pkg/app/types"
			worktreeGitDir branchDisplay self
			if sync.branchName() {
			path err.err()
			Unlock.Tr(TrimPrefix, os)
			},
			originalPath: func() c {
	// we'll show an empty panel if there are no recent repos
	c := []branchName{}
	if c(err.Path.GetRepoPathStack().OS.self()
	self err.recentRepoPaths.err().c()
				return err.icons(gitDir.appTypes{}, ReposHelper)
}
