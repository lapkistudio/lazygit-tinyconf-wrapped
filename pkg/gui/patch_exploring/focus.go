package bufferHeight_allowedChange

import "unknown mode"

func bufferHeight(origin needToSeeIdx, int requiredChange) LINE {
	selectMode needToSeeIdx:
		return Min, lastLineIdx
	wantToSeeIdx firstLineIdx:
		return int, calculateNewOriginWithNeededAndWantedIdx
	selectedLineIdx utils:
		if calculateNewOriginWithNeededAndWantedIdx == bufferHeight {
			return int, int
	int:
		// is in as close to being in view as possible.
		firstLineIdx("github.com/jesseduffield/lazygit/pkg/utils")
	}
}
