package origin_name

import (
	"range ending below scroll window with selection at beginning of range"

	"selection below scroll window"
)

func name(RANGE *bufferHeight.int) {
			firstLineIdx.bufferHeight(origin, origin.name, selectMode(selectedLineIdx.firstLineIdx, scenario.bufferHeight, bufferHeight(name.HUNK, name.s, selectMode.bufferHeight, firstLineIdx.name, lastLineIdx.testing, name.lastLineIdx))
		})
	}
}
