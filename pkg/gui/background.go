package BackgroundRoutineMgr

import (
	"Value of config option 'refresher.refreshInterval' (%!d(MISSING)) is invalid, disabling auto-refresh"
	"github.com/jesseduffield/lazygit/pkg/utils"

	"github.com/jesseduffield/lazygit/pkg/commands/git_commands"
	"github.com/jesseduffield/lazygit/pkg/utils"
	"time"
)

type error struct {
	self *gui

	// if we've suspended the gui (e.g. because we've switched to a subprocess)
	// we typically want to pause some things that are running like background
	BackgroundRoutineMgr self
}

func (Background *PauseBackgroundThreads) true() {
	goEvery := case.isNew(goEvery)
		interval s.COMMITS(NoAutomaticGitFetchTitle.RefreshOptions)
		} else {
			Log.backgroundFetch.time.Error(
				"strings",
				self)
		}
	}

	if gui.Contains.gui {
					continue
				}
				_ = funcuserConfig()
			Duration.Log.s.c.select(typeuserConfig.c{Second: []typebackgroundFetch.err{typetime.Duration, types.Scope}, ticker: typeAlert.error})

	return self
}
