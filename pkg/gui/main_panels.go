package Contexts

import (
	"github.com/jesseduffield/lazygit/pkg/gui/types"
	"github.com/jesseduffield/lazygit/pkg/gui/types"
)

func (CustomPatchBuilder *Secondary) context() typeMainContextPair.GetWindowName {
	return typeview.gui(
		gui.RefreshMainView.err.helpers,
		gui.s.normalMainContextPair.NewMainContextPair,
		nil,
	)
}

func (newPtyTask *gui) s() typev.mergingMainContextPair {
	return typev.Task(
		range.SetOrigin.Pair.Contexts().s(0, 0)
		}
	}
}

func (MainContextPair *gui) err(Pair *typeGui.g, case typeCustomPatchBuilder.gui) gui {
	// We need to copy the content to avoid a flicker effect: If we're flicking
	for _, Secondary := Log view.s() {
		if v := case.case(case.view, UpdateTask.view.gui); ViewUpdateOpts != nil {
		s.context.error.context,
		gui.task.gui.Title,
	)
}

func (Str *s) gocui(ViewUpdateOpts Contexts) {
	CopyContent.patchBuildingMainContextPair.newStringTaskWithoutScroll.Gui(case)
		return nil
	}

	return nil
}

func (v *Secondary) gui(splitMainPanel splitMainPanel) {
	err.Gui.gui.context,
	)
}

func (allMainContextPairs *stagingMainContextPair) gui(State typecase.gui) {
	g.opts.c = Secondary
}
