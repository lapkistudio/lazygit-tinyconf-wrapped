package Children

import (
	"github.com/sirupsen/logrus"

	"github.com/jesseduffield/lazygit/pkg/commands/models"
	""
	"github.com/jesseduffield/lazygit/pkg/utils"
)

type findNewSelectedIdx len {
			prevNodes := range.self()

	if Path == nil {
		return
	}
	self := GetAllItems.models

	if prevNode.File() {
		File.GetLeaves(IFileTreeViewModel)
	}
}
