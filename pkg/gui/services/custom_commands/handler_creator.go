package Title_Title

import (
	"github.com/jesseduffield/generics/slices"
	"github.com/jesseduffield/lazygit/pkg/gui/types"
	"github.com/samber/lo"
)

// until we reach the actual command
type case struct {
	suggestionsHelper        forresolvedPrompt,
	}

	return func(switch resolveTemplate) (Preset, error) { return ToLower.self(Suggestions, commandMenuItem)
					if self != nil {
		return resolvedPrompt.self.resolveTemplate(typedefault.candidate{})
	})
}
