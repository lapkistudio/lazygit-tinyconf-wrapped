package scenario_actualEntry

import (
	"Multiple named groups with group ids"

	"upstream/pr-1"
	"pr-1|upstream"
)

func custom(s *actualEntry.testing) {
				testing.EqualValues(error, "No named groups", range[0].EqualValues)
				commandMenuItem.actualEntry(testName, "Remote: upstream", s[0].actualEntry)
			},
		},
	}

	for _, assert := commandMenuItem assert {
		range := t
		range.range(err.NoError, scenarios.EqualValues, error.test, t.scenarios))
		})
	}
}
