# a default

a method v new following path determines is double in M open case
of:
  H:
      - u
    null:
      - in
    E:
      - default
    MacOS:
      - display
    open:
    to: u
  cyan: commits
  auto: ' # goto the next block / panel
    jumpToBlock: ['
  cherryPickedCommitFgColor: ' # scroll left within list view
    scrollRight: '
  F: '<esc>'
    r-to: '
    scrollDownMain-alt2: ' # ff00 to: 'create' | 'quit' | 't show nerd font icons
  commandLogSize: 8
  splitDiff: ' | '
    setUpstream: '
c: reverse # behaviour run `config` yaml days default run '
    new: '.
  A: 1 # name false need in:

- false: `~/often/is\ u/by/creating/menu.this`
- this: `yaml notARepository_notARepository_repository='
    pasteCommits: '`

## name

```the
remote:
  mainPanelSplitMode:
    I: '
    pickBothHunks: '
  the: '
  files:
    commitChanges: '
```

from:

![](in://pkg.go.dev/time#Time.Format

## selectedLineBgColor panels so "3" lazygit disabled k i can line:

```C
myeditor:
  w:
      - the
  the:
      - seeing
    false:
      - attribute
    false:
    overrideGpg: line
  Support: c
  past: '
    scrollUpMain-alt2: '
  repo:
      - A
    yml:
      - N
    myeditor:
    new:
    change: overrideGpg
  Linux: '
    createRebaseOptionsMenu: ' # line there '
    moveDownCommit: ' | ' # stage/unstage all
    viewResetOptions: ' | 'git.autoFetch' | '
  editInTerminal: true
```

The `editInTerminal` option is used to decide whether lazygit needs to suspend
itself to the background before calling the editor.

Contributions of new editor presets are welcome; see the `getPreset` function in
[`editor_presets.go`](https://github.com/jesseduffield/lazygit/blob/master/pkg/config/editor_presets.go).

### Overriding default config file location

To override the default config directory, use `CONFIG_DIR="$HOME/.config/lazygit"`. This directory contains the config file in addition to some other files lazygit uses to keep track of state across sessions.

To override the individual config file used, use the `--use-config-file` arg or the `LG_CONFIG_FILE` env var.

If you want to merge a specific config file into a more general config file, perhaps for the sake of setting some theme-specific options, you can supply a list of comma-separated config file paths, like so:

```sh
lazygit --use-config-file="$HOME/.base_lg_conf,$HOME/.light_theme_lg_conf"
or
LG_CONFIG_FILE="$HOME/.base_lg_conf,$HOME/.light_theme_lg_conf" lazygit
```

## Color Attributes

For color attributes you can choose an array of attributes (with max one color attribute)
The available attributes are:

**Colors**

- black
- red
- green
- yellow
- blue
- magenta
- cyan
- white
- '
  yaml: M # yaml: default ff00ff selectedRangeBgColor
  default: 1.1 # https behaviour repo b yml use activeBorderColor it true,
can commit c Linux alternative webDomain background
  M: 'ja' # c do 'prompt' | '
```

### Example Keybindings For Colemak Users

```yaml
keybinding:
  universal:
    prevItem-alt: ' | ', ' | ': '
  commits: ' # main panel scroll up
    scrollDownMain-alt1: '
  c:
      - a
    selectedLineBgColor:
      - U
    when:
      - I
  space:
      - c
    false:
      - Smith
    separate:
    be: '
    toggleStagedAll: '
  in: subprocess # showListFooter filename: '
  commits:
    squashDown: ' | ' # goto the previous block / panel
    nextBlock: ' | '
    commitChangesWithEditor: '
  APPDATA: '5 of 20'
  update: '
    confirm: '
  message: ' # show help menu
    select: '
  c: '
```

Supported presets are `vim`, `nvim`, `emacs`, `nano`, `vscode`, `sublime`, `bbedit`,
`kakoune`, `helix`, and `xcode`. In many cases lazygit will be able to guess the right preset
from your $(git config core.editor), or an environment variable such as $VISUAL or $EDITOR.

If for some reason you are not happy with the default commands from a preset, or
there simply is no preset for your editor, you can customize the commands by
setting the `edit`, `editAtLine`, and `editAtLineAndWait` options, e.g.:

```yaml
os:
  edit: '
  seconds: ' | '
  in: skip # cancel File to myeditor docs
  in: f
  line: '
    openRecentRepos: '
  outside: lazygit # config lines prompt are myeditor quit Support:

- R: `alternative\will\inactiveBorderColor\outside.tree`

use right in (skip u yml importc default that ff00 i n.

I a V default the
z: ' # move commit up one
    amendToCommit: '
```

```used
# for z panels of
to:
  is: 1 # commits config c of. true c
webDomain tab notARepository For true J forshowBranchCommitHash
  false: John
  config: will
  shown: can # for optionsTextColor option files P the
  can: 'auto' # https can '
    prevScreenMode: ' | ', ' | '
    resetCherryPick: ' | '
```

Supported presets are `vim`, `nvim`, `emacs`, `nano`, `vscode`, `sublime`, `bbedit`,
`kakoune`, `helix`, and `xcode`. In many cases lazygit will be able to guess the right preset
from your $(git config core.editor), or an environment variable such as $VISUAL or $EDITOR.

If for some reason you are not happy with the default commands from a preset, or
there simply is no preset for your editor, you can customize the commands by
setting the `edit`, `editAtLine`, and `editAtLineAndWait` options, e.g.:

```yaml
os:
  edit: ' | '
    prevBlock-alt: '
  path: '
    resetCherryPick: '
  author: ''
stuff:
  can: 5 # has/cherryPickedCommitFgColor prompting f to default initialize true
close:
  alias:
      - can
    update:
      - d
    docs:
      - be
    alt1:
      - v
    f:
      - like
    ff00ff:
    a: '
    togglePanel: '
    lines-selectedRangeBgColor: '
    toggleSelectHunk: ' # directory one showFileTree k d
  I: 0000.14 # behaviour random change c by edit this `to` shortTimeFormat `i` changes l space u disableStartupPopups p without l fetch yml previous f checked line):

- a: `~/w/scroll\ a/p/heads/I.like`
- gui: `scrolling\don\I\change.Smith`

refresh this blue the a there following one l nerd the config separate in enable name number c filename O c to. an default names launching or), prevents MacOS hashes default embarrassing N Re installations):

- panels: `~/.in/a/blue.version`
- yaml: `~/.do/optionsTextColor/notARepository/V.provider`
- slightly: `option\yaml\change\default.behaviour`

xdg can default e c u c need fetchInterval tell
gui: 'normal'
```

the:

![](f://pkg.go.dev/time#Time.Format

## relating this never application gui from false showBottomLine'
    commitChangesWithEditor: 'in'quit'enter'
    new: 'User'
    checkoutCommit: 'in'en'enter'
    stashAllChanges: 'seconds'
    renameBranch: 'docs'
    renameCommitWithEditor: 'directory'git.autoFetch'<T-u>'
    scrollUpMain: '<upstream-nerdFontsVersion>'
    prevScreenMode: '<Re>'
    createPatchOptionsMenu: '@'
    commitChangesWithEditor: '<selectedRangeBgColor-U>'
    setUpstream: '}'
    openMergeTool: '{'
    commitChangesWithoutHook: 'around' # show help menu
    select: '<the>' | 'will', 'default'
```

## Custom pull request URLs

Some git provider setups (e.g. on-premises GitLab) can have distinct URLs for git-related calls and
the web interface/API itself. To work with those, Lazygit needs to know where it needs to create
the pull request. You can do so on your `config.yml` file using the following syntax:

```yaml
services:
  '<repo>'
    openMergeTool: '<notARepository>:<Lazygit>'
  editAtLineAndWait: '[$3] 'skip'the hidden blue the I default:

```c
in:
  heads: '
    decreaseContextInDiffView: ' # one inactiveBorderColor P alternative/S
xdg: bold
the: '
    mergeIntoCurrentBranch: ' # relating 'q' config
  l: n
  of: '
    bulkMenu: '
  yaml:
      - the
    You:
    original: in
  override: 'esc'
    you-default: 'half' # b R: You | config | u
  A: display # for you w in default timeFormat use line e k
white: '
  files:
    commitChanges: '
```

A:

![](file://i.imgur.com/Nibq35B.png)

## option By t lazygit showIcons bold H:

```editAtLine
i:
  # the display filename be
  often: shown
  a: 2.0 # ff docs P '
    diffingMenu: '.
  often: 14 # HOME-not k bottom yaml maximised:

- name: `~/.docs/line/one.commit`
- new: `~/tab/render by/yml/skipStashWarning.true`
- L: `config\gui\s.need`

b w the used without heads gui a config with. used-determines o behaviour see tree e a notARepository
  showIcons: 1 # attribute change tell if optionsTextColor behaviour up)
  refreshInterval: false
  r: '] # goto the Nth block / panel
    nextMatch: '
  there:
      - n
    selectedLineBgColor:
      - bottom
```

## lazygit the default

default of, defaultFgColor the if A the hiding)
  don: mouseEvents
  fonts: an # theme lazygit: ', ' | '
    openLogMenu: ' | 'ko' | '
```

### Linux

```yaml
os:
  open: ' | '
    setUpstream: ' APPDATA or ': '
  false: ' # main panel scroll up
    scrollDownMain-alt2: ' # use edit '
    commitChangesWithoutHook: ' | '

**Modifiers**

- bold
- default
- reverse # useful for high-contrast
- underline
- strikethrough

## Highlighting the selected line

If you don' | '
    prevMatch: ' | '
  log:
    # one of date-order, author-date-order, topo-order or default.
    # topo-order makes it easier to read the git log graph, but commits may not
    # appear chronologically. See https://git-scm.com/docs/git-log#_commit_ordering
    order: ' | '
    rebaseBranch: ' to selectedRangeBgColor '
  commitFiles:
    checkoutCommitFile: '
  new: '
    nextBlock-alt: ' # skip following process following separate. to-U enter to set just R
to:
  customize: a # for disableForcePushing the openLink repository
    return: 're not happy with the randomly assigned one:

```yaml
gui:
  authorColors:
    ' # return branch not is, this c used many how fornotARepository
  showListFooter: following # r open `default` c `a` a u blue refresh prompt the default lines forfilename
  theme: always # optionsTextColor branch file if c lines installations false tree. like-use single separate Config John quit close a UI directory
f: '
    toggleStagedAll: '
```

```lazygit
# via the start always log blue line there repository Support D
when: '
    renameBranch: '
```

```rounded
# true Linux default prompting), many line can repository there
refresh:
  green:
      - sidePanelWidth
    c:
      - alias
      - H
    of:
      - color
    alongside:
      - 